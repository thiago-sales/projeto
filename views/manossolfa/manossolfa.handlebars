<h1 style="text-align:center" class="card">Manossolfa Digital</h1>

<h5 style="text-align:center">Instruções: faça os sinais demonstrados na figura e veja a cifra na tela!</h5>

<!--Imagem Manossolfa-->
<div style="text-align:center">
    <img src="https://terradamusicablog.com.br/wp-content/uploads/2016/07/MANOSSOLFA.jpg">
</div>

</br>
</br>

<a href=" https://thiago-sales.github.io/manossolfa/" 
  class="btn btn-primary" 
  style="margin-left: 500px;">Clique aqui</a>



{{!-- 

    <!-- teachable machine, Manossolfa Digital -->

<script type="text/javascript">
	//link do mmodelo no teachable machine:       https://teachablemachine.withgoogle.com/models/WTgRj7Lz3/


//<div>Teachable Machine Image Model - p5.js and ml5.js</div>
//<script src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/0.9.0/p5.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/0.9.0/addons/p5.dom.min.js"></script>
<script src="https://unpkg.com/ml5@latest/dist/ml5.min.js"></script>
<script type="text/javascript">
// Classifier Variable
  
// Teachable Machine
// The Coding Train / Daniel Shiffman
// https://thecodingtrain.com/TeachableMachine/1-teachable-machine.html
// https://editor.p5js.org/codingtrain/sketches/PoZXqbu4v

// The video
let video;
// For displaying the label
let label = "waiting...";
// The classifier
let classifier;
let modelURL = 'https://teachablemachine.withgoogle.com/models/WTgRj7Lz3/';

// STEP 1: Load the model!
function preload() {
  classifier = ml5.imageClassifier(modelURL + 'model.json');
}

function setup() {
//lembrar de alterar o canvas (640, 520)
  createCanvas(1350, 520);
  // Create the video
  video = createCapture(VIDEO);
  video.hide();

  // STEP 2: Start classifying
  classifyVideo();
}

// STEP 2 classify the videeo!
function classifyVideo() {
  classifier.classify(video, gotResults);
}

function draw() {
  background(0);

  // Draw the video
  image(video, 350, 10);

  // STEP 4: Draw the label
  textSize(32);
  textAlign(CENTER, CENTER);
  fill(255);
  text(label, width / 2, height - 16);
  
}

// STEP 3: Get the classification!
function gotResults(error, results) {
  // Something went wrong!
  if (error) {
    console.error(error);
    return;
  }
  // Store the label and classify again!
  label = results[0].label;
  classifyVideo();
}

</script>
 --}}
